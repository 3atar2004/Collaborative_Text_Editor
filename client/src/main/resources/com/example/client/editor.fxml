<!--
<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/8.0.171" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.client.EditorController" stylesheets="@styles.css">

    <HBox style="-fx-background-color: #90c6f1;" alignment="CENTER_LEFT" padding="10">
        <Label fx:id="titleLabel" style="-fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold;"/>
    </HBox>

    <HBox>
        <VBox style="-fx-padding: 10; -fx-background-color: #eaf3fc;" prefWidth="200" spacing="10">
            <HBox padding="5" spacing="10">
                <Button text="Import" styleClass="action-button"/>
                <Button text="Export" styleClass="action-button"/>
            </HBox>

            <HBox spacing="5" alignment="CENTER_LEFT">
                <Label text="Editor Code:" style="-fx-font-size: 12px;"/>
                <TextField fx:id="editorCodeField" style="-fx-background-color: #e6e6e6; -fx-font-size: 12px;"
                           prefWidth="120" editable="false"/>
                <Button fx:id="copyEditorButton" text="Copy" style="-fx-font-size: 12px; -fx-padding: 2 10 2 10;
                          -fx-background-color: #90c6f1; -fx-text-fill: white;"/>
            </HBox>

            <HBox spacing="5" alignment="CENTER_LEFT">
                <Label text="Viewer Code:" style="-fx-font-size: 12px;"/>
                <TextField fx:id="viewerCodeField" style="-fx-background-color: #e6e6e6; -fx-font-size: 12px;"
                           prefWidth="120" editable="false"/>
                <Button fx:id="copyViewerButton" text="Copy" style="-fx-font-size: 12px; -fx-padding: 2 10 2 10;
                          -fx-background-color: #90c6f1; -fx-text-fill: white;"/>
            </HBox>

            <HBox spacing="5" alignment="CENTER">
                <Button fx:id="undoButton" text="↩" style="-fx-font-size: 16px; -fx-background-color: #90c6f1;
                          -fx-text-fill: white; -fx-background-radius: 50%; -fx-min-width: 30px; -fx-min-height: 30px;"/>
                <Button fx:id="redoButton" text="↪" style="-fx-font-size: 16px; -fx-background-color: #90c6f1;
                          -fx-text-fill: white; -fx-background-radius: 50%; -fx-min-width: 30px; -fx-min-height: 30px;"/>
            </HBox>

            <Label text="Active Users:"/>
            <ListView fx:id="userList" style="-fx-background-color: #ffffff; -fx-border-color: #dcdcdc;
                      -fx-border-radius: 4; -fx-padding: 5;"/>

            <Button fx:id="endButton" text="End Session" styleClass="action-button" maxWidth="Infinity"/>
        </VBox>

        <HBox fx:id="textEditorContainer" HBox.hgrow="ALWAYS">
            <ScrollPane fx:id="lineScrollPane" hbarPolicy="NEVER" vbarPolicy="NEVER" prefWidth="40"
                        style="-fx-background-color: #eaf3fc; -fx-border-color: transparent;">
                <VBox fx:id="lineNumbers" padding="5 0 0 5" style="-fx-background-color: #eaf3fc;"/>
            </ScrollPane>

            <ScrollPane fx:id="textScrollPane" fitToWidth="true" fitToHeight="true">
                <TextArea fx:id="textArea" style="-fx-control-inner-background: #fdfdfd; -fx-font-family: 'Consolas';
                            -fx-font-size: 14px; -fx-border-color: #c0c0c0; -fx-border-radius: 6; -fx-focus-color: #4682b4;"/>
            </ScrollPane>
        </HBox>
    </HBox>
</VBox>-->

<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/8.0.171" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.client.EditorController" stylesheets="@styles.css">

    <HBox styleClass="header-box" alignment="CENTER_LEFT">
        <Label fx:id="titleLabel" styleClass="title-label"/>
    </HBox>

    <HBox>
        <VBox styleClass="sidebar" prefWidth="200" spacing="10">
            <HBox padding="5" spacing="10">
                <Button text="Import" styleClass="action-button" onAction="#handleImport"/>
                <Button text="Export" styleClass="action-button" onAction="#handleExport"/>
            </HBox>

            <HBox spacing="5" alignment="CENTER_LEFT">
                <Label text="Editor Code:" styleClass="label"/>
                <TextField fx:id="editorCodeField" styleClass="text-field" prefWidth="120" editable="false"/>
                <Button fx:id="copyEditorButton" text="Copy" styleClass="button" onAction="#copyEditorCode"/>
            </HBox>

            <HBox spacing="5" alignment="CENTER_LEFT">
                <Label text="Viewer Code:" styleClass="label"/>
                <TextField fx:id="viewerCodeField" styleClass="text-field" prefWidth="120" editable="false"/>
                <Button fx:id="copyViewerButton" text="Copy" styleClass="button" onAction="#copyViewerCode"/>
            </HBox>

            <HBox spacing="5" alignment="CENTER">
                <Button fx:id="undoButton" text="↩" styleClass="undo-redo-button" onAction="#undo"/>
                <Button fx:id="redoButton" text="↪" styleClass="undo-redo-button" onAction="#redo"/>
            </HBox>

            <Label text="Active Users:" styleClass="label"/>
            <ListView fx:id="userList" styleClass="list-view"/>

            <Button fx:id="endButton" text="End Session" styleClass="action-button" maxWidth="Infinity" onAction="#endSession"/>
        </VBox>

        <HBox fx:id="textEditorContainer" HBox.hgrow="ALWAYS">
            <ScrollPane fx:id="lineScrollPane" hbarPolicy="NEVER" vbarPolicy="NEVER" prefWidth="40" styleClass="scroll-pane">
                <VBox fx:id="lineNumbers" styleClass="line-numbers"/>
            </ScrollPane>

            <ScrollPane fx:id="textScrollPane" fitToWidth="true" fitToHeight="true">
                <TextArea fx:id="textArea" styleClass="text-area"/>
            </ScrollPane>
        </HBox>
    </HBox>
</VBox>